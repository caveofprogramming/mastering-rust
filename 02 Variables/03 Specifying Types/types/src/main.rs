fn main() {
    let celsius:f64 = 24.5;

    let fahrenheit:f64 = celsius * 9.0 / 5.0 + 32.0;

    println!("Fahrenheit: {fahrenheit}");
}

/*
 * Integer types
 * signed: i8, i16, i32, i64, i128
 * unsigned: u8, u16, u32, u64, u128
 * usize
 *
 * Floating point types
 * f32, f64
 */
